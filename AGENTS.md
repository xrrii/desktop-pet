# PetDock Desktop 开发约定

1. 默认中文输出；代码标识保持项目命名习惯，方法和函数添加中文注释，复杂逻辑补充内部说明。
2. 先读现有代码和文档；不得臆造业务规则、接口或文件路径。可自行处理的实现选择继续推进，业务歧义集中向用户说明。
3. 必要位置添加中文日志，禁止记录密钥、Token、Cookie、用户正文和附件正文。
4. 文件和脚本优先采用 UTF-8 无 BOM；保留已有未提交修改，不覆盖其他任务成果。
5. Managed Service v1 的权威源位于 `petdock-cloud/contracts/managed-service/v1`；本仓库只消费整体快照，不独立修改协议。
6. 开发文档从 [文档索引](docs/README.md) 进入。

## 多代理协作

**并发硬性上限：同一任务及其跨仓子任务合计最多两个代理，包含主代理。主代理始终占用一个名额，同时最多一个未结束的子代理；等待中的主代理或子代理仍计入。超出上限的任务排队，子代理禁止再派生子代理。**

用户要求子代理或并行协作时，主代理先读 [角色手册](docs/guides/multi-agent/README.md)，按需选择角色。角色文件是协作提示词，不代表已注册应用内代理类型。

分派时提供 [共同约定](docs/guides/multi-agent/common.md)、对应角色文件和填写后的 [任务单](docs/guides/multi-agent/task-template.md)。同一文件同一时间只有一名写入负责人；主代理保留整合、验收和交付责任。小任务可由主代理直接完成。
