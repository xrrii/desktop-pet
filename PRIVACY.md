# 隐私说明

本说明适用于当前 PetDock 本地 BYOK 版本。规划中的官方托管服务尚未上线，不在
本说明范围内；上线前必须发布独立的用户协议和隐私政策。

## 本地保存的数据

PetDock 会在 Electron `userData` 目录保存用户主动产生的配置和运行数据，包括：

- 桌宠和界面设置；
- 对话、记忆候选、工具记录和实际引用的网页来源；
- 用户授权导入的附件副本、知识库索引和生成文件；
- 已安装 Skill 的清单和文件；
- 本地向量索引及用户主动下载的 Embedding 模型。

项目不会把上述本地文件提交到源码仓库。删除会话、知识库、Skill 或生成文件时，
只处理 PetDock 管理的副本和索引，不删除用户原始文件。

## API Key

主模型、视觉、在线 Embedding 和搜索服务的 API Key 使用 Electron `safeStorage`
加密后保存在本机用户数据目录。Renderer 只接收是否已配置的脱敏状态，不会回填
密钥明文。密钥只在调用用户选择的服务或启动本地 Runtime 时使用。

## 发送到第三方的数据

只有用户主动配置并启用在线能力时，PetDock 才会向对应服务发送数据：

- 在线对话模型：当前对话、必要的系统上下文、用户选择的附件内容、知识库命中
  片段和工具结果；
- 视觉模型：去除 EXIF/GPS 后生成的受限图片派生图；
- 在线 Embedding：需要生成向量的文本片段和查询；
- 联网搜索：用户问题派生的搜索关键词；读取网页正文时还会访问用户可见的公开
  URL。

第三方如何保存和处理这些数据由其条款和隐私政策决定。用户不应向未获授权的
在线服务发送机密、个人敏感或受合同限制的数据。

## 日志

默认日志记录运行状态、错误类型、耗时和脱敏标识，不应记录 API Key、完整对话
正文、附件正文或本地绝对路径。排查问题时分享日志前仍应进行人工检查。

## 用户控制

用户可以在应用中关闭在线能力、删除已保存的 API Key，以及删除 PetDock 管理的
会话、知识库、Skill、附件和生成文件。彻底卸载前，如需清除全部本地数据，还应
删除操作系统中 PetDock 对应的 `userData` 目录。

第三方服务清单及数据范围参见 `THIRD_PARTY_SERVICES.md`。
